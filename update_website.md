# How to Update Public Website

Make changes in content directory. Run `hugo server` to preview the changes. If everything looks good, run `hugo` command which will generate static pages. Then commit the `public` submodule (visible in VSCode source control) and then commit `main`. The website should be upto date in a minute.
